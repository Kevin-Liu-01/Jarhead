import type { ReactElement } from "react";

/** The one mono line under the desk (design.md §2.1). */
export function DeskCaption(): ReactElement {
  return <p className="desk-caption">menu bar 33 · notch 185×32 · island 420×184 · drawn at 1:1 · the Console is the app&apos;s own harness render</p>;
}
